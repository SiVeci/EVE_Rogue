// Shared tier -> color lookup for module tier badges (market listings,
// hangar/sell rows, combat loot display, fitting-window lock hints).
export const TIER_COLORS = { T1: 'var(--color-text-muted)', Meta: '#e8a838', T2: '#5a96ff' };
